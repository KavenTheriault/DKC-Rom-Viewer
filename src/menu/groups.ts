import { MainMenuGroup } from '../types/layout';

export const generalGroup: MainMenuGroup = {
  label: 'General',
  items: [],
};

export const otherGroup: MainMenuGroup = {
  label: 'Other',
  items: [],
};
